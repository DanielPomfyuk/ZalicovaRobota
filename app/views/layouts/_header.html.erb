<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-normal">Home sweet Home</h5>
  <nav class="my-2 my-md-0 mr-md-3">
    <span class="p-2 text-dark" ><%=link_to "Home",root_path%>  </span>
    <span class="p-2 text-dark" ><%=link_to "Make an ad",new_ad_path%>  </span>
  </nav>
  <% if user_signed_in? %>
    <span class="p-2 text-dark" > <%= link_to 'Edit Profile', edit_profile_path(current_user) %></span>
    <span class="btn btn-outline-primary" ><%=link_to "Log out" , destroy_user_session_path , method: :delete %></span>
<% else %>
  <span class="btn btn-outline-primary" ><%=link_to "Log in" , new_user_session_path %></span>
  <%end %>>
  </div>
